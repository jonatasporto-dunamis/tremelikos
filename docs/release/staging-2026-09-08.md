# Evidências de Release — Staging & Banco (FINAL)

## 1. Identificação
- **Branch**: `fix/next16-hardening`
- **Commit SHA**: `d35b421ed80c05b77d12dd17e8651b3cb527b68f`
- **Data/Hora**: 2026-09-08T13:23Z
- **Ambiente**: Staging (Local) + Banco Remoto (Supabase)

## 2. Gates Locais
- `npm audit`: **0 vulnerabilidades**
- `npm run lint`: **0 erros**
- `npm test`: **190/190 testes passando**
- `npm run build`: **Sucesso**

## 3. Banco de Dados (Supabase)
- **Backup**: Snapshot lógico salvo em `/opt/tremelikos/.local-backups/` antes das alterações.
- **Conexão**: `.env.local` ignorado pelo Git; conexão via `SUPABASE_DB_URL` validada.

### Migrations Aplicadas
- **008**: `idx_orders_store_cart_unique` criado e validado.
- **009**: Colunas `manual_pause`, `delivery_fee`, `delivery_min_minutes`, `delivery_max_minutes`, `delivery_areas` adicionadas em `stores`.
- **010**: Helper `current_admin_store_id()` criado; 17 policies scoped aplicadas; políticas antigas (`is_admin()`) removidas das tabelas de comércio.

### Validação SQL
- `stores` NÃO possui `store_id` (correto).
- Tabelas filhas (`products`, `orders`, etc.) possuem `store_id` (correto).
- Policies scoped (`%scoped%`) presentes em todas as tabelas admin.

## 4. Status de Deploy
- **Container**: `tremelikos-app-1` (Up 4 dias).
- **Produção**: Código atual não refletido no container (requer rebuild/deploy via CI/CD).
- **Staging Local**: Valido com sucesso (portas 3010).
- **API Status**: Endpoint `/api/store/status` validado logicamente via testes unitários e E2E local.

## 5. Decisão Final

### GO para Produção (Condicionado)

**Critérios atendidos**:
1. Segurança e Hardening implementados (CSP, Rate Limit, RLS Scoping).
2. Banco de dados migrado e validado com sucesso (008, 009, 010).
3. Todos os testes locais verdes.

**Ação necessária**:
1. Executar o deploy do container (`tremelikos-app-1`) para refletir a branch `fix/next16-hardening`.
2. Após o rebuild, validar o smoke teste final em `https://tremelikos.growthpulse.com.br/api/store/status`.

**Riscos Residuais**:
- Tabelas `analytics_events` e `audit_logs` ainda possuem policies `is_admin()` amplas (globais), o que é aceitável para logs globais.
